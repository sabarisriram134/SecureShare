{
  "name": "secureshare-contracts",
  "version": "1.0.0",
  "description": "SecureShare smart contracts",
  "main": "scripts/deploy.js",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js"
  },
  "devDependencies": {
    "hardhat": "^2.17.0",
    "@nomicfoundation/hardhat-toolbox": "^3.0.0"
  },
  "dependencies": {
    "ethers": "^6.9.0"
  }
}
